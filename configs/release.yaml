schema_version: 1

# Exact configuration for the location-neutral controlled release.
run:
  name: release
  mode: controlled_experiment
  seed: 20260901
  output_dir: artifacts/release
  overwrite: true

market:
  name: Location-neutral controlled market
  exit_horizon_weeks: 17

data:
  source_mode: generated_fixture
  generated_property_count: 1200
  split:
    train_fraction: 0.60

simulator:
  training_seed: 1129
  evaluation_seed: 4079
