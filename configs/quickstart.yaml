schema_version: 1

# Every setting below is consumed by the executable controlled experiment.
run:
  name: quickstart
  mode: controlled_experiment
  seed: 20260901
  output_dir: artifacts/quickstart
  overwrite: true

market:
  name: Location-neutral controlled market
  exit_horizon_weeks: 17

data:
  source_mode: generated_fixture
  generated_property_count: 1200
  split:
    train_fraction: 0.60

simulator:
  training_seed: 1129
  evaluation_seed: 4079
