schema_version: 1

# Every setting below is consumed by the executable workflow.
run:
  name: quickstart
  mode: semi_synthetic
  seed: 20260901
  output_dir: artifacts/quickstart
  overwrite: true

market:
  name: Phoenix-Mesa-Chandler
  exit_horizon_weeks: 17

data:
  source_mode: generated_fixture
  generated_property_count: 1200
  split:
    train_fraction: 0.60

simulator:
  training_seed: 1129
  evaluation_seed: 4079
